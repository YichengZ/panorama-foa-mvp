# Historical Multi-Agent Start Prompt

The original multi-agent implementation flow is complete. Do not use this file
as the current server execution prompt.

For server handoff, use `docs/SERVER_HANDOFF_MMAUDIO.md`.

Current repo:

```text
https://github.com/YichengZ/panorama-foa-mvp
```

Current server task:

```text
install local MMAudio dependencies and run the starship cabin smoke test with
--audio-provider mmaudio
```
