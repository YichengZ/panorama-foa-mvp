# Install the MVP package from the subproject:
#
#   cd panorama_foa_mvp
#   python -m pip install -e ".[test]"
#
# Local MMAudio GPU dependencies are environment-specific and should be
# installed from the sibling third_party/MMAudio checkout in the server
# validation workspace. They are intentionally not part of the default MVP
# test dependency set.
